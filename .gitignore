# Editor
.cursor/
.vscode/

# Environment
.env*
.vercel

# Google OAuth client
client_secret*.json

# Dependencies
node_modules/
/pnpm-lock.yaml

# Build
*.tsbuildinfo

# Local umbrella installer (anti-slop). Only the repo-root file.
# Do not ignore nested package.json — Railway/Docker clones cannot see them.
/package.json
/oxlint.config.ts
