# MDX files in content directory have special formatting requirements
# that conflict with standard prettier formatting
content/

# Generated by `apps/api` `pnpm run openapi:export` (raw JSON.stringify output).
# Prettier reformats arrays inline, which breaks the CI "verify committed spec"
# step (it diffs against the export script's output). Leave it untouched.
openapi.json
agent-openapi.json
pnpm-lock.yaml

# Other common files to ignore
node_modules/
.next/
dist/
build/
*.log