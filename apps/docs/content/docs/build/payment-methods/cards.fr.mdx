---
title: 'Comment accepter les cartes ?'
description: 'Choisissez le checkout hébergé pour la plupart des flux carte, ou la collecte embarquée si votre app doit contrôler toute l’interface.'
docType: how-to
---

import { Callout } from '@/components/docs/docs-callout';
import { DocsScreenshot } from '@/components/docs/docs-screenshot';
import { DocsAgentIndex } from '@/components/docs/docs-agent-index';

<DocsAgentIndex />

Pour la plupart des équipes, les paiements carte devraient commencer par le checkout hébergé. Utilisez la collecte embarquée uniquement si votre produit doit afficher la saisie carte dans sa propre interface.

## En bref

| | |
| --- | --- |
| Approbation | 3DS hébergé si l’émetteur l’exige |
| Règlement | Après `completed` ; certains réglages carte retardent `available_at` |
| Direct vs hébergé | Hébergé. `POST /charge/card` renvoie `503` |
| Remboursement | Transactions carte `completed` |
| Min / max | - |

Mêmes colonnes que [Canaux de paiement](/build/payment-channels#channel-capabilities).

Les cartes fonctionnent en **checkout hébergé** (le plus rapide) ou en flux embarqué avec [Payment Elements](/build/accept/payment-elements) et `POST /charge/card`.

<DocsScreenshot name="build/cards" alt="Formulaire carte sur le checkout hébergé lomi." />

## Quel flux carte choisir ?

| Flux | À utiliser quand |
| --- | --- |
| Checkout hébergé | Vous voulez l’expérience carte la plus rapide et sûre |
| Liens de paiement | Vous voulez une URL partageable compatible carte |
| Charge carte embarquée | Vous contrôlez l’UI checkout et la confirmation client |
| Payment Elements | Vous voulez des primitives UI carte réutilisables dans votre app |

## Checkout hébergé (recommandé)

1. Créez une session checkout.
2. Le client choisit **Carte** sur la page lomi.
3. Gérez la 3D Secure si nécessaire.
4. Vérifiez via webhooks avant livraison.

## Charge carte embarquée

<Callout type="warn">
  Les charges carte embarquées (`POST /charge/card`) et Switch (`POST /charge/switch`) ne sont pas encore disponibles et renvoient actuellement `503 service_unavailable`. Utilisez le [checkout hébergé](/build/accept/checkout) pour les cartes.
</Callout>

1. `POST /charge/card` → `client_secret`.
2. Confirmez avec Payment Elements.
3. Webhook ou polling pour le statut final.

Voir [API Charges](/api/charge/ChargesController_createCardCharge) et l’[app de référence charge directe](/build/accept/direct-charges#reference-implementation).

## Cartes test

| PAN | Comportement |
| --- | --- |
| `4242 4242 4242 4242` | Succès |
| Autres | [Bac à sable: cartes](/start/sandbox-payments#testing-card-payments) |

<Callout type="warn">
  N’utilisez jamais de vraies cartes en test ni de numéros test en live.
</Callout>

## Notes de sécurité

- Ne manipulez jamais de clés secrètes dans un navigateur ou une app mobile.
- Traitez les valeurs `client_secret` comme des capacités sensibles et temporaires.
- Utilisez les clés publiables uniquement quand le flux carte le demande.
- Confirmez l’état final via webhooks ou lectures API serveur.

<DocsNextSteps>
  <DocsNextStep href="/build/accept/checkout" hint="Collecte carte hébergée">
    Checkout hébergé
  </DocsNextStep>
  <DocsNextStep href="/build/accept/payment-elements" hint="UI carte sur votre site">
    Payment Elements
  </DocsNextStep>
  <DocsNextStep href="/build/reliability/verify-payments" hint="Ne jamais se fier au client">
    Vérifier les paiements
  </DocsNextStep>
  <DocsNextStep href="/start/sandbox-payments" hint="Cartes de test">
    Paiements sandbox
  </DocsNextStep>
</DocsNextSteps>
