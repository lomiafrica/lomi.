---
title: "Get merchant account balance for a currency"
description: "Get merchant account balance for a currency"
full: true
method: get
path: /merchants/{id}/balance
operationId: MerchantsController_getBalance
---

## Aperçu

Get merchant account balance for a currency

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /merchants/&#123;id&#125;/balance` (Get merchant account balance for a currency).




### À savoir

Exige `currency_code` (XOF, USD ou EUR).

### Voir aussi

[Account balances](/api/balances/AccountsController_getBalance)

## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /merchants/{id}/balance`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | - | Merchant UUID |

### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `currency_code` | query | Oui | - |  |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | - |
| `400` | Missing currency_code |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/merchants/value/balance?currency_code=XOF" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## OpenAPI

- **operationId**: `MerchantsController_getBalance`
- **Operation**: `GET /merchants/{id}/balance`

Schémas complets et **Try it** : [référence API](/api).
