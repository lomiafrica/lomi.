---
title: "Créer un webhook"
description: "Créer un webhook"
full: true
method: post
path: /webhooks
operationId: WebhooksController_create
---

## Aperçu

Créer un webhook

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `POST /webhooks` (Créer un webhook).




### À savoir

Stockez le secret de signature de façon sûre ; vérifiez la signature sur chaque requête entrante.

### Voir aussi

[List webhooks](/api/webhooks/WebhooksController_findAll) · [Test webhook](/api/webhooks/WebhooksController_test)

## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`POST /webhooks`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._


### Paramètres de requête

_Aucun paramètre de requête._


### Corps de la requête

_Pas de corps JSON pour cette opération._


## Réponses

| Statut | Description |
| --- | --- |
| `201` | Webhook créé |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X POST "https://sandbox.api.lomi.africa/webhooks" \
  -H "X-API-KEY: $LOMI_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## OpenAPI

- **operationId**: `WebhooksController_create`
- **Operation**: `POST /webhooks`

Schémas complets et **Try it** : [référence API](/api).
