---
title: "List team"
description: "List team"
full: true
method: get
path: /team
operationId: TeamController_list
---

## Aperçu

List team

Membres et invitations de l’organisation.

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /team`.

## Authentication

Les routes marchandes exigent une clé API dans `X-API-KEY` (voir [Aperçu](/api)). Clé **test** sur `https://sandbox.api.lomi.africa`, clé **live** sur `https://api.lomi.africa`.

## Endpoint

`GET /team`

Base URLs:

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Example

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/team" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._

### Paramètres de requête

_Aucun paramètre de requête._

### En-têtes

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `Lomi-Version` | header | Non | - | Optional schema version pin. Echoes OpenAPI info.version (currently 1.2.0). Routes stay unversioned. |

## Réponses

| Statut | Description |
| --- | --- |
| `200` | - |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## OpenAPI

- **operationId**: `TeamController_list`
- **Operation**: `GET /team`

Full schemas and **Try it**: [API reference](/api). Machine-readable contract: repo `apps/docs/openapi.json`.
