---
title: "Mettre à jour un abonnement"
description: "Mettre à jour un abonnement"
full: true
method: patch
path: /subscriptions/{id}
operationId: SubscriptionsController_update
---

## Aperçu

Mettre à jour un abonnement

Met à jour le statut ou les métadonnées (ex. pause). Les champs tarifaires restent gérés par le système.


### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `PATCH /subscriptions/&#123;id&#125;` (Mettre à jour un abonnement).



### Voir aussi

[Retrieve subscription](/api/subscriptions/SubscriptionsController_findOne) · [Cancel subscription](/api/subscriptions/SubscriptionsController_cancel)

## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`PATCH /subscriptions/{id}`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | - | UUID de l’abonnement |

### Paramètres de requête

_Aucun paramètre de requête._


### Corps de la requête

_Pas de corps JSON pour cette opération._


## Réponses

| Statut | Description |
| --- | --- |
| `200` | Abonnement mis à jour |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X PATCH "https://sandbox.api.lomi.africa/subscriptions/value" \
  -H "X-API-KEY: $LOMI_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## OpenAPI

- **operationId**: `SubscriptionsController_update`
- **Operation**: `PATCH /subscriptions/{id}`

Schémas complets et **Try it** : [référence API](/api).
