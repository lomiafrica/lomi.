# lomi. payout mapping (Zcash lab)

Same freeze as Stellar. Shielded receive + memo/`payout_id` reconcile. Last mile Wave/MTN/SPI. Merchants never hold keys. No production `rail`.

Do not file ZCG until `data/testnet-proof.json` has `status: shielded`.
