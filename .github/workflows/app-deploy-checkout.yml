name: lomi · deploy · checkout

on:
  repository_dispatch:
    types: [lomi-deploy-checkout]
  push:
    branches: [main]
    paths:
      - "apps/checkout"
      - "apps/checkout/**"
      - "packages/**"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "tooling/scripts/install-app-with-packages.mjs"
      - "tooling/vercel/checkout.json"
      - ".github/workflows/app-deploy-checkout.yml"
      - ".github/workflows/app-deploy-vercel.yml"
      - ".github/actions/checkout-app-submodule/**"
  workflow_dispatch:
    inputs:
      ref:
        description: Branch to deploy
        type: string
        default: main

concurrency:
  group: lomi-deploy-checkout-${{ github.event.client_payload.pr_number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  require-check:
    uses: ./.github/workflows/app-require-check.yml

  deploy:
    needs: require-check
    uses: ./.github/workflows/app-deploy-vercel.yml
    with:
      app: checkout
      repository: lomiafrica/checkout
      submodule-path: apps/checkout
      working-directory-mode: umbrella
      workflow-ref: ${{ inputs.ref }}
    secrets:
      REPO_CHECKOUT_PAT: ${{ secrets.REPO_CHECKOUT_PAT }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_CHECKOUT }}
